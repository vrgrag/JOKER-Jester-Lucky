// ============================================================
// INSIGHT ENV — Microsoft Clarity project id
// ============================================================
// [FINGERPRINT] The Clarity project id is UNIQUE per project — never
// reuse another project's id (see .cursor/rules/clarity_analytics.mdc
// §1). If you copy this file into a new app, rotate the value below
// before shipping.
//
// Verbose SDK logging is only enabled in debug builds — a release
// build stays silent (LogLevel.None) inside `Insight.config`.
// ============================================================

const String kJesterClarityProjectId = 'xnave2qgfl';
